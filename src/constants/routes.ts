const HOME = "/";
const CHANNEL = "/channels/me";
const REGISTER = "/register";
const LOGIN = "/login";
const FRIENDS = "/channels/friends";
const NITRO = "/nitro";
const SHOP = "/shop";

const routes = {
  HOME,
  CHANNEL,
  REGISTER,
  LOGIN,
  FRIENDS,
  NITRO,
  SHOP,
};

export default routes;
